import bg from "../../assets/images/home/3.jpg"
const Audience = () => {
    return (
        <section>
            <div className="bg-fixed bottom-0 h-[500px] bg-cover bg-center mt-20 flex justify-center" style={{
            backgroundImage: `url(${bg})`,
          }}>
            </div>
        </section>
    );
};

export default Audience;